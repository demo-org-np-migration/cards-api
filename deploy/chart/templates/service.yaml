apiVersion: v1
kind: Service
metadata:
  name: cards-api
  labels:
    {{- include "cards-api.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "cards-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
